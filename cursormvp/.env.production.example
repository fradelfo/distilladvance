# =============================================================================
# Production Environment Variables Template
# =============================================================================
# Copy this file and set values in:
# - Railway (API service environment variables)
# - Vercel (Project settings → Environment Variables)
# - GitHub (Repository → Settings → Secrets and variables → Actions)
# =============================================================================

# -----------------------------------------------------------------------------
# General
# -----------------------------------------------------------------------------
NODE_ENV=production

# -----------------------------------------------------------------------------
# Database (Railway PostgreSQL)
# -----------------------------------------------------------------------------
# Get this from Railway: Project → PostgreSQL → Variables → DATABASE_URL
DATABASE_URL=postgresql://user:password@host:5432/railway

# -----------------------------------------------------------------------------
# API Server (Railway)
# -----------------------------------------------------------------------------
API_PORT=3001
API_SECRET=<generate-with: openssl rand -hex 32>

# -----------------------------------------------------------------------------
# Web App (Vercel)
# -----------------------------------------------------------------------------
# Your Vercel deployment URL
NEXTAUTH_URL=https://your-app.vercel.app
# Your Railway API URL
NEXT_PUBLIC_API_URL=https://your-api.up.railway.app

# -----------------------------------------------------------------------------
# Authentication
# -----------------------------------------------------------------------------
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=<generate-32-char-random-string>
AUTH_SECRET=<same-as-nextauth-secret>

# Google OAuth (https://console.cloud.google.com)
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-your-client-secret

# -----------------------------------------------------------------------------
# AI Services
# -----------------------------------------------------------------------------
# OpenAI (https://platform.openai.com/api-keys)
OPENAI_API_KEY=sk-proj-your-openai-key
OPENAI_MODEL_DEFAULT=gpt-4-turbo-preview
OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# Anthropic (https://console.anthropic.com)
ANTHROPIC_API_KEY=sk-ant-your-anthropic-key
ANTHROPIC_MODEL_DEFAULT=claude-sonnet-4-20250514

# -----------------------------------------------------------------------------
# Vector Database (Optional - for semantic search)
# -----------------------------------------------------------------------------
# ChromaDB URL if using external instance
CHROMA_URL=http://localhost:8000

# -----------------------------------------------------------------------------
# Monitoring (Optional)
# -----------------------------------------------------------------------------
# Sentry (https://sentry.io)
SENTRY_DSN=
SENTRY_ENVIRONMENT=production

# -----------------------------------------------------------------------------
# GitHub Secrets Required
# -----------------------------------------------------------------------------
# These should be set in GitHub → Repository → Settings → Secrets:
#
# VERCEL_TOKEN          - Vercel account token
# VERCEL_ORG_ID         - Vercel organization/team ID
# VERCEL_PROJECT_ID     - Vercel project ID
# RAILWAY_TOKEN         - Railway account token
# DATABASE_URL          - Railway PostgreSQL connection string
# NEXTAUTH_SECRET       - Auth secret (same as above)
# API_SECRET            - API secret (same as above)
# OPENAI_API_KEY        - OpenAI API key
# ANTHROPIC_API_KEY     - Anthropic API key
# GOOGLE_CLIENT_ID      - Google OAuth client ID
# GOOGLE_CLIENT_SECRET  - Google OAuth client secret
# API_URL               - Railway API URL (for health checks)
# WEB_URL               - Vercel web URL (for health checks)
